<form method="post" action="{{ path('admin_contenu_pages_delete', {'id': contenu_page.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contenu_page.id) }}">
    <button class="btn">Delete</button>
</form>
